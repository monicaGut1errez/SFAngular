import { CustomTooltipColoresDirective } from './custom-tooltip-colores.directive';

describe('CustomTooltipColoresDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTooltipColoresDirective();
    expect(directive).toBeTruthy();
  });
});
